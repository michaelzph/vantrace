{
  "name": "@glassbox/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "glassbox": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.tsx --format esm --no-dts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@glassbox/core": "workspace:*",
    "ink": "^5.0.0",
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0"
  }
}
